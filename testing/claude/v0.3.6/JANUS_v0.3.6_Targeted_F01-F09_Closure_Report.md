# JANUS Governance Challenge Harness v0.3.6
# Targeted V035-F01 through V035-F09 Closure Report

Test date: 2026-09-25 (America/New_York)
Scope: targeted retest of V035-F01 through V035-F09 only. This is not a full regression.
Method: runtime checks (F01, F02, F03) ran against the live GitHub Pages deployment in Chrome. Repository checks ran on an unmodified clone of `main`. Mutation probes ran on temporary copies outside the repository.
Repository changes: none. HEAD is still `b7f51e604f2d3c9c09174a8e412130b08319dd2f` and the working tree is clean. No commits, tags or file edits were made.
Raw evidence: `JANUS_v0.3.6_targeted_closure_raw_evidence.txt`, which holds the suite outputs, the 34 mutation kill reasons and the supplemental probe results.

---

## Phase 0: exact deployed artifact identity

| Item | Expected | Observed (live deployment) | Result |
|---|---|---|---|
| Deployed version (HTML) | 0.3.6 | `Version 0.3.6` | MATCH |
| build-info.json `version` | 0.3.6 | 0.3.6 | MATCH |
| Build ID | janus-governance-challenge-harness-v0.3.6 | identical | MATCH |
| Bound code commit | 6a065001b39947ea50bced91d31336171c56c42a | identical | MATCH |
| Release HEAD / `main` | b7f51e604f2d3c9c09174a8e412130b08319dd2f | `git ls-remote origin main` gives an identical value | MATCH |
| Deployed app.js SHA-256 | 641b66c7cb19356500a063184fdef68fd8b3dc9ddf48ceafe6648a5f43018a9f | identical, and equal to the manifest entry | MATCH |
| Deployed JANUS source SHA-256 | 21766f5dbd86b728f4cb7e5794b208dab52d169eb4e0d4fd717ae8c374551fe4 | identical | MATCH |
| Authored scenario-block SHA-256 | 948536a17a454e59f782e023ac1ded8e30e9f33ca660c3dbd674273fe1b56488 | identical (17,033 bytes, computed from deployed app.js with the specified method) | MATCH |
| Manifest entry count | 49 | 49 | MATCH |

Additional identity corroboration:
- The only change between `6a06500` and `b7f51e6` is `build-info.json`.
- The deployed build-info.json is byte-identical to `b7f51e6:build-info.json` (the SHA-256 prefix `c75d65995973f1e7` matches).
- All 49 manifest files were fetched from the live site and hashed: 49 of 49 match. One request for `docs/EXPECTED_CONTRACT_HASHES.json` returned a transient HTTP 503. It matched on 3 of 3 retries.
- The live canonical dispositions are 01 BLOCK_REAUTHORIZE, 02 BLOCK, 03 BLOCK_REAUTHORIZE, 04 BLOCK_REAUTHORIZE, 05 INSUFFICIENT_SPECIFICATION and 06 INSUFFICIENT_SPECIFICATION. This matches the authoring invariant.

**IDENTITY GATE: PASS.** Testing continued.

---

## Finding table

| Finding | Original issue | Targeted tests performed | Evidence observed | Status | Residual risk / notes |
|---|---|---|---|---|---|
| **V035-F01** | Blank-rendering fillers (U+2800, U+3164, U+FFA0, U+115F, U+1160) behaved incorrectly in duplicate detection. | **Deployed runtime:** in the live page, called `validateScenarioContract` on clones of scenario 01 for each of the 5 fillers. Checks: (A) `"Policy A B"` vs `"Policy A<F>B"` as sources and as commitments; (B) `"Policy AB"` vs `"Policy A<F>B"` as sources and as commitments; (C) `"Policy   A\t\nB"` vs `"Policy A B"`; (D) NFC `Café` vs `Cafe+U+0301` in sources and commitments. **End-to-end UI:** injected a U+2800 source duplicate and a U+3164 commitment duplicate into the live scenario, then clicked Run. (E) Searched README, CHANGELOG, VERSION, index.html and the gate for "visual" equivalence claims. | For all 5 fillers: `canonicalComparisonText("Policy A<F>B")` = `"Policy A B"`. Source duplicate was rejected ("…duplicate citations") and commitment duplicate was rejected ("…duplicate entries"). The `"Policy AB"` comparison returned `null` (accepted) in both contexts, so removing a filler causes no false equivalence. Whitespace collapse caught the duplicate. NFC variants were rejected in both contexts. The UI Run was refused with `Execution refused: Scenario source list contains duplicate citations.` / `…commitments contain duplicate entries.`, with no result and no lastRun. No visual-equivalence overclaim exists; the wording is limited to "canonical" / "specified canonically equivalent". | **CLOSED** | Minor: the local suite checks the no-false-equivalence case (B) for sources only, not commitments. The deployed test covered commitments and they behaved correctly. Not actionable. |
| **V035-F02** | U+007F could count as meaningful content or bypass duplicate detection. | **Deployed runtime:** rationale set to `"\u007F"` and to `"\u007F\u007F ⠀"`. Source duplicates tested with U+007F as a suffix and mid-string. A commitment duplicate tested with embedded U+007F. A source and a commitment each consisting only of U+007F. Checked `canonicalComparisonText("a\u007Fb")`. End-to-end: live UI Run with a U+007F-only rationale and with a U+007F-suffixed duplicate source. | Rationale rejected: `Scenario contract field is invalid: rationale`. Every U+007F duplicate variant was rejected as a duplicate. A U+007F-only source gave "source list is invalid" and a U+007F-only commitment gave "invalid support status or text". Canonical form `"ab"` shows U+007F is stripped. UI Run was refused in both cases, with no evidence produced. | **CLOSED** | None. |
| **V035-F03** | A late Export invalidation race: state changes during awaited hashing after the early guard still downloaded the old artifact and cleared the notice. | **Deployed runtime (live page).** Intercepted `URL.createObjectURL` and anchor `.click()` to count download attempts; no files were actually saved. Established Run and Replay CONSISTENT, then started Export. **(1)** Wrapped `deriveReplayRecord`, called only after Export's early guard (`app.js` L918), and during it performed a prediction change, a scenario change, or Reset. **(2)** Wrapped `recordSnapshotHash` and triggered a prediction change on its 4th call during Export, which is the final awaited hash (L926) immediately before the late guard. **(3) Negative control:** evaluated a copy of the deployed `exportEvidenceCore` with only the late guard (L952) removed, then repeated (2). **(4)** Clean Run, Replay and Export afterwards. **Local:** confirmed the regression delays digest #5, the first digest after the early guard. Ran the regression 10 times and the late-guard-removed mutant 10 times. | (1) Prediction, scenario and Reset each produced **0 downloads**. lastRun = null and Export was disabled. The notice stayed visible ("Prediction changed…" / "Scenario changed…"). Reset showed its intended clean-slate state: no evidence and no artifact. (2) **0 downloads**, notice kept. (3) The negative control reproduced the original defect: **a download occurred and the notice was cleared**. This proves the test reached the post-early-guard window and that the late guard is the control that closes it. (4) The clean exports downloaded normally (`…evidence-change-block-…-v0.3.6.json`, `…learning-authority-block_reauthorize-…-v0.3.6.json`). Local: 10 of 10 passes and 10 of 10 kills, with the kill reason `Late invalidated Export still produced an artifact.` | **CLOSED** | The local race test depends on timing (a 35 ms delay against a 10 ms trigger), but it was stable in 10 of 10 runs. Not actionable. |
| **V035-F04** | Significant mutations survived the regression gate. | Ran `v0.3.6-mutation-regression.mjs` and read all 34 operators. Re-ran them through an instrumented copy that records each kill's thrown assertion, to check that each class is killed by the intended oracle. Checked that the Export-lock mutant targets `exportEvidence()`, not `replayLastRun()`, and that the Replay and Export race tests enter awaits after the early guards. Ran 7 supplemental equivalent-form mutants against the targeted classes. On the deployed page, checked that the event_log control is live by tampering `event_log`, recomputing both stored hashes, then exporting. | Preflight passed with 34 of 34 active, and 34 of 34 were killed. All 15 required classes were killed, 14 of them by the intended behavioral assertion. Examples: S05 ESCALATE is killed by the scenario-block hash; the Export lock by `Rapid Export calls produced duplicate artifacts`, and the operator does mutate `exportEvidence()`; the late guard by `Late invalidated Export still produced an artifact`; filler deletion, U+007F and whitespace each by their specific assertion. Supplemental probes: removing only U+FFA0 or only U+1160 from the filler map was **killed**; dropping only `exportInFlight` was **killed**; late guard changed to `if(false)` was **killed**. **Gap:** class 10 ("Export event_log validation forced true") is killed only by a source-text `includes()` check (regression L552), not by exercising the control. The equivalent mutant `event_log: … === … \|\| true` keeps that string and **SURVIVED**. On the deployed page the control is live and named in the refusal: `Evidence integrity check failed (event_log, authored_state_integrity, replay_rederived_consistent)`. | **PARTIALLY CLOSED** | **Low.** For class 10 the oracle checks that the code text is present, not how it behaves. Export is still refused through `authored_state_integrity`, so there is no runtime exposure. The gap is the test oracle only: a behavioral assertion that the refusal names `event_log` would close it. Two other mutants survived, "late guard weakened to `!lastRun`" and "Replay guard weakened to `!lastRun`". Both are **equivalent mutants** under the current code, because every invalidation path nulls `lastRun` and Run is locked during Replay/Export. Not actionable. |
| **V035-F05** | Documentation tests depended on one exact sentence and allowed other integrity overclaims. | Ran the doc audit. Checked the deployed UI and the README positive disclosures. Ran the 6 required mutation types (A–F) plus variants of each against `v0.3.6-doc-audit.mjs`. | **Positive assertions are present and enforced.** README: unsigned client-side artifact; "PASS is not a digital signature or proof of authenticity"; page/DevTools control plus recomputed hashes; "accidental or uncoordinated mutation, not malicious coordinated forgery". Deployed UI: "Evidence exports are unsigned client-side artifacts… not a digital signature or proof against a user who controls DevTools/console state and can recompute client-side hashes." (A) DevTools disclosure deleted: KILLED. (B) inverted to "is a digital signature": KILLED. (C) UI limitation removed: KILLED. **(D) tamper-proof: only the form "is/are/remains tamper-proof" is caught.** These SURVIVED: README "Tamper-proof evidence export.", README "The harness provides tamper-proof evidence.", UI "Tamper-proof evidence." **(E) Version 1.0:** "Version 1.0 ready." inside the README v0.3.6 section was KILLED. These SURVIVED: "This release is ready for Version 1.0." in the same section, "Version 1.0 approved." at the top of the README, "Version 1.0 validated." in index.html, and "Version 1.0 approved." appended to VERSION.txt. **(F) independent closure:** the literal "Independent closure confirmed." added to the README v0.3.6 section with the disclaimer kept **SURVIVED**. Also SURVIVED: "V035-F01 through V035-F09 were independently closed.", "Independent closure has been confirmed." at the top of the README, and "Independent closure confirmed." in CHANGELOG. | **PARTIALLY CLOSED** | **Medium.** The current documentation is accurate: it has no overclaims and all positive disclosures. But the overclaim protections still depend on narrow phrasings and on location (README v0.3.6 section only; UI only for tamper-proof; CHANGELOG, VERSION and index.html are not checked for V1.0 or closure claims). The finding's own example, "independent closure confirmed", goes undetected. Reproduce with `probe.mjs`; the outputs are in the raw evidence file. |
| **V035-F06** | Historical release gates pointed to superseded "current" gates. | Read the heading and every `V0_3_x_RELEASE_GATE.md` reference in the v0.3.2, 0.3.3, 0.3.4 and 0.3.5 gates and in the v0.3.6 gate. Checked the README current-gate reference. | Each historical gate starts with `# SUPERSEDED — historical … gate`, says it "must not be used for current releases", and points only to `V0_3_6_RELEASE_GATE.md` (1 gate reference each). No historical gate presents itself as current. `V0_3_6_RELEASE_GATE.md` is the current gate and lists `v0.3.6-regression/doc-audit/mutation-regression/manifest-audit.mjs`. The README names V0_3_6 as the current gate. Pointer-regression and superseded-removal mutants are both killed. | **CLOSED** | None. |
| **V035-F07** | Release history implied findings were closed earlier than they were. | Read the CHANGELOG, README history sections and VERSION.txt against items A–F. | (A) CHANGELOG v0.3.2: "partially addressed V031-NEW-07 …". (B) "independent v0.3.3 testing later confirmed full observability closure". (C) v0.3.4 "was superseded after its deployed smoke test found residual issues; it was not promoted to Version 1.0" in CHANGELOG, with equivalent wording in README. (D) v0.3.5 "passed its deployed smoke gates but its final independent regression found nine actionable findings". (E) v0.3.5 "superseded and … not promoted to Version 1.0" in both files. (F) v0.3.6 says "Independent closure is **not** claimed…" and VERSION says "Independent closure: pending". The v0.3.5 CHANGELOG line "names the current v0.3.5 suites/gate" is accurate as of that entry. | **CLOSED** | Note: F05 above shows a future closure overclaim would not be caught automatically. That is covered under F05; the current wording is accurate. |
| **V035-F08** | The build manifest left out tracked, served and evidence files, and its scope was ambiguous. | Listed `git ls-tree -r` at `6a06500`. Excluded only build-info.json. Compared the set against `b7f51e6:build-info.json`. Recomputed all hashes from the bound-commit blobs and from the HEAD worktree. Hashed all 49 files as served live. Checked the file categories. Read README lines 87, 164 and 231. | 50 files tracked, 49 expected, 49 in the manifest. **Missing: none. Extra: none.** 0 hash mismatches against the bound commit and 0 against HEAD. Live: 49 of 49 match. Categories: served assets/images (1), JANUS source docx (1), test suites (13 `.mjs`), gates and results (11 `V0_3_*`), preserved independent evidence (14 under `testing/`), README, CHANGELOG, VERSION, index.html, styles.css. README L87 separates three things: full tracked-file provenance coverage except build-info.json, runtime enforcement of manifest version, build ID and served `app.js`, and a separate JANUS source-hash check. The other manifest hashes are described as provenance metadata. | **CLOSED** | Non-actionable: by design and as documented, only app.js and the source document are re-hashed at runtime. |
| **V035-F09** | Automated evidence did not record the exact source and scenario hashes or the scenario-block hashing method. | Read `tests/V0_3_6_AUTOMATED_REGRESSION_RESULT.txt`. Read regression L109–L115. Reproduced both hashes independently with Python on the repo and with WebCrypto on the deployed page. Ran `git diff e0e6790 e447db4` on the v0.3.5 result. | (A) Source SHA recorded, with observed equal to expected. (B) Block SHA recorded. (C) Method recorded exactly: "const scenarios = [" through "];" plus exactly one trailing LF, with no other bytes. (D) The regression recomputes the block from `app.js` (`indexOf('\n];') + 4`, which is equivalent to the stated method) and asserts the hash; it does not read the evidence file. Both hashes were reproduced independently, giving a 17,033-byte block. (E) The v0.3.5 diff is append-only. Apart from a trailing-newline change on the final `PASS` line, it adds only the "POST-REGRESSION HASH REPRODUCTION ADDENDUM", which says it "does not change the original automated PASS result … or the later independent FAIL gate result". | **CLOSED** | None. |

---

## TARGETED CLOSURE TOTALS

CLOSED: 7 (F01, F02, F03, F06, F07, F08, F09)
PARTIALLY CLOSED: 2 (F04, F05)
OPEN: 0
UNTESTABLE: 0

**ACTIONABLE FINDINGS REMAINING: 2**

---

## RELEASE-GATE RESULT

**TARGETED CLOSURE: FAIL — ACTIONABLE FINDINGS REMAIN**

### Remaining actionable findings

**1. V035-F05 (residual): overclaim protections are narrow and depend on location. Severity: Medium.**
Reproduction: copy the files the doc audit reads into a temporary root, apply one edit, and run `JANUS_TEST_ROOT=<tmp> node tests/v0.3.6-doc-audit.mjs`. It exits 0 (PASS) for each of these:
- README, after `## v0.3.6 bounded hardening candidate`: add `Independent closure confirmed.` (the finding's own example; the existing disclaimer stays)
- README: add `V035-F01 through V035-F09 were independently closed.`
- README top: add `Independent closure has been confirmed.`, or `Version 1.0 approved.`
- README v0.3.6 section: add `This release is ready for Version 1.0.`
- CHANGELOG under `## v0.3.6`: add `Independent closure confirmed.`
- index.html: add `Version 1.0 validated.` or `Tamper-proof evidence.`
- VERSION.txt: append `Version 1.0 approved.`
- README: add `Tamper-proof evidence export.` or `The harness provides tamper-proof evidence.`

Cause: the regexes in `v0.3.6-doc-audit.mjs` need a copula (`is|are|remains tamper-proof`, `independent closure (is|has been) …`) or a fixed word order (`Version 1.0 (ready|…)`). They are also applied only to the README v0.3.6 section, plus index.html for tamper-proof. CHANGELOG, VERSION.txt and index.html are not checked for Version 1.0 or closure claims.

**2. V035-F04 (residual): the Export event_log oracle is static. Severity: Low.**
Reproduction: in a temporary copy, change `app.js` L941 to `event_log: JSON.stringify(expectedLog) === JSON.stringify(snapshot.event_log) || true`, then run `JANUS_TEST_ROOT=<tmp> node tests/v0.3.6-regression.mjs`. It exits 0 (PASS). The listed operator is killed only by `appText.includes(…)` at regression L552, so the control is never exercised. There is no runtime exposure: on the deployed page the control is live and refuses as `(event_log, authored_state_integrity, replay_rederived_consistent)`. The gap is test-oracle quality.

Do not promote to Version 1.0. After these two items are corrected, repeat this targeted closure. The final full independent zero-open-finding adversarial regression against the exact deployed artifact is still required after that.

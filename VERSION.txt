JANUS Governance Challenge Harness

Version: 1.0
Build ID: janus-governance-challenge-harness-v1.0
Status: release candidate

Release status:
- Version 1.0 candidate constructed from the independently tested deployed v0.3.14 release
- deployed v0.3.14 passed all runtime, scenario, provenance, replay, export, integrity, invalidation, and fail-closed checks
- deployed v0.3.14 returned one Low documentation finding, V0314-F01
- V0313-F01 therefore remained not closed under the zero-actionable-finding rule
- Version 1.0 is not approved, tagged, or released until this exact deployed candidate independently returns zero actionable findings

Release-state governance:
- the release boundary is the complete exact Git code/content commit and tree, not a hand-selected claim-surface list
- all tracked paths and bytes are part of that bound code/content state
- only build-info.json and docs/BOUND_RELEASE_STATE.json may differ in the single provenance commit after the bound code commit
- newly added, removed, renamed, mode/type-changed, or byte-changed tracked files therefore change the governed release state automatically
- all untracked release-workspace files are refused, including paths hidden by .gitignore, .git/info/exclude, or core.excludesFile
- build-info.json and docs/BOUND_RELEASE_STATE.json must each use exact canonical JSON bytes
- release-state binding is a workflow declaration, not authentication, a digital signature, deployment enforcement, or proof that a human reviewed the release
- final release audit requires Commit B to be the single direct child of Commit A and to change exactly both provenance-only files

V0312-F01 closure (v0.3.13):
- docs/EXPECTED_CONTRACT_HASHES.json no longer presents 0.3.9 as an active release version
- contracts_last_changed_in records that the authored contract hashes last changed in v0.3.9
- all six contract hashes are unchanged

V0313-F01 / V0314-F01 closure work:
- the current release gate and control-suite identity are explicit rather than copied from a historical release
- historical control-suite lists are labelled historical instead of current
- VERSION.txt preserves V0312-F01 under its correct historical attribution
- CHANGELOG.md begins with its document title
- the current documentation audit checks these structural conditions rather than merely requiring a finding ID to appear somewhere
- final closure is not claimed until the exact deployed v1.0 candidate independently returns zero actionable findings

Authored scenario content: unchanged.
JANUS source document: unchanged.
Canonical scenario dispositions: unchanged.

Version 1.0 release requires:
1. successful v1.0 local gates;
2. exact deployed-artifact verification;
3. independent confirmation that V0314-F01 and V0313-F01 are closed;
4. zero open Critical, High, Medium, or Low actionable findings on the exact deployed candidate;
5. confirmation that the original governance-ask assessment remains YES;
6. only then may the exact qualified artifact be tagged and released as Version 1.0.
